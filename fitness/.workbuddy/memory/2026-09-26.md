# 2026-09-26

## 每日 AI 健康日记（自动化 b7257233）
- 状态：**日记已生成并归档，但未发布**——smoke.js 出现 2 处 FAIL，按自动化规则「有 FAIL 就不发布」停在 publish 之前。
- 产物：insight.json（aiNarrative 450 字 / aiAdvice 5 条）+ diary/2026-09.md（第 5 条）。本地已持久化，明日同步会被 build_insight.py 继承，不会丢。
- 本地状态分 87（excellent），腕表准备度 68，缺口由 3 张回 19。
- 核心发现：昨天预告的 ACWR 归零兑现——172.2 滚出 7 天窗口，acute 24.6→0，ratio 2.25→0.00。旧账到期不是恢复。chronic 仍 10.9，10/3 前后 123.7 到期会再跌。

## smoke.js 两处 FAIL（根因已定位，待 Warden 定夺）
1. `ACWR 概览 0 字符` + `ACWR 迷你走势 32 字符` —— **真 bug**：assets/app.js:953 `if (!L || !L.ratio)`，ratio 恰为 0 时 0 是 falsy，整块走「无训练负荷数据」分支。实际有数据（chronic 10.9 / load28 306.5）。最小修法：`!L.ratio` → `L.ratio == null`。
2. `今日高亮标记 0 个` —— 数据时序误报，非 bug。app.js:673 只在有睡眠记录的日期格加 `.sg-today`，今早 9/26 睡眠未同步（series 末行 sleepH=null），故无格子可高亮。等睡眠同步后自动恢复，类同每日 08:00 的「活动量偏低」误报。

## 复用经验（本轮新增）
- **字数**：首稿 556 → 494 → 481 → 460 → 448，压了四轮才进线。目测低估幅度本轮约 25%，再次验证「先写完再 `len()` 实量，预留 3–4 轮压缩」。
- **apply_ai.py 写的是 `aiNarrative` / `aiAdvice` / `aiModel` / `aiAt` 四个字段，不是 `narrative`**；`narrative` 保持规则引擎版本（narrativeSource=rule）。校验是否写入成功要看 ai* 字段，别查 `ai` 或 `narrative`。
- **ACWR 归零这类「指标因窗口滚动而突变」的日期必须提前一天预警**（9/25 已预警，9/26 兑现）——否则极易被误读成状态变好。
- wellness.json 已无 loadAerobicLow / loadTargetLow / loadFeedback 字段（9/22 时还有），advice 里别再引用「有氧负荷 128 vs 下限 212」，改用 28 天训练次数与 28 天有氧 TE（9.0）当锚点。
